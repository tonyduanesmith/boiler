import { createMuiTheme } from '@material-ui/core/styles'
const theme = createMuiTheme({
	palette: {
		// primary: {
		// 	light: red[500],
		// 	main: red[500],
		// 	dark: red[500]
		// },
		// secondary: {
		// 	light: green[500],
		// 	main: green[500],
		// 	dark: green[500]
		// },
		// text: {
		// 	primary: grey[700],
		// 	secondary: '#fff'
		// }
	},
	overrides: {}
})

export default theme
